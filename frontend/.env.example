# Origin of the Go API (used by Next.js rewrites for /api/*).
# Local dev: npm run dev with API on the host
BACKEND_ORIGIN=http://127.0.0.1:8080
# Docker Compose sets BACKEND_ORIGIN=http://api:8080 at image build time (see frontend/Dockerfile).
